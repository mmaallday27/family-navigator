node_modules
dist
dist-ssr
*.local

# Secrets — the navigator's API key lives here, never in the repo
.env
.env.local

# Local database + uploaded document bytes (per-device dev data)
server/data/

# TS composite build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
